package car.spetification;

public class ArraysOfColors {

    public Color[] addColorsForArray() {
        Color[] colors = new Color[]{Color.BLACK, Color.GREEN, Color.RED, Color.WHTIE, Color.YELLOW};
        return colors;
    }
}
